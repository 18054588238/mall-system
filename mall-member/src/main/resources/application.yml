spring:
  application:
    name: mall-member
  datasource:
    username: root
    password: rootroot
    url: jdbc:mysql://localhost:3306/mall-ums?useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 60000
      maximum-pool-size: 10
  #  cloud:
#    nacos:
#      discovery:
#        server-addr: 127.0.0.1:8848
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto   #主键自增

server:
  port: 8020