spring:
#  application:
#    name: mall-product
  datasource:
    username: root
    password: rootroot
    url: jdbc:mysql://localhost:3306/mall-pms?serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  thymeleaf:
    cache: false # 关闭thymeleaf缓存,不然热部署不起作用
  redis:
    host: 127.0.0.1
    port: 6379
  cache:
    type: redis
    redis:
      time-to-live: 60000 # 缓存过期时间ttl 60s
#      key-prefix: yyqx
#      use-key-prefix: true
      cache-null-values: true # 是否缓存空值，防止缓存穿透
  session:
    redis:
      namespace: spring:session
    store-type: redis
  cloud:
    sentinel:
      transport:
        dashboard: 127.0.0.1:8858
        port: 8719
        client-ip: 192.168.0.102
# ??????????
#  cloud:
#    nacos:
#      discovery:
#        server-addr: 127.0.0.1:8848
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto   #????
#      logic-delete-field: showStatus
#      logic-delete-value: 0
#      logic-not-delete-value: 1
server:
  port: 8040

management:
  endpoints:
    web:
      exposure:
        include: '*'
feign:
  sentinel:
    enabled: true #打开sentinel对feign对支持