spring:
#  application:
#    name: mall-ware
  datasource:
    username: root
    password: rootroot
    url: jdbc:mysql://localhost:3306/mall-wms?serverTimezone=Asia/Shanghai&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  cloud:
    sentinel:
      transport:
        dashboard: 127.0.0.1:8080
        port: 8719
        client-ip: 192.168.0.102
#  cloud:
#    nacos:
#      discovery:
#        server-addr: 127.0.0.1:8848
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto   #????
server:
  port: 8050

management:
  endpoints:
    web:
      exposure:
        include: '*'
